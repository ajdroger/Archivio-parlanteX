# Frontend — copy to .env.local (gitignored)
#
# Opzione A (default in dev): relative URL + Vite proxy → http://localhost:9080
# VITE_API_BASE_URL=/api
#
# Opzione B: chiamata diretta al PHP Gateway Docker
VITE_API_BASE_URL=http://localhost:9080/api
